===
.CCA. Simple if/else
===
{ x > 0:
	~ y = x - 1
- else:
	~ y = x + 1
}
---
(ink
	(content_block
		(paragraph
			(cond_block
				(cond_arm
					(binary (identifier) (number))
					(code (assignment name: (identifier) value: (binary (identifier) (number)))))
				(cond_arm
					(else)
					(code (assignment name: (identifier) value: (binary (identifier) (number)))))
			)
		)
	)
)


===
.CCB. Extended if/else
===
{
	- x == 0:
		~ y = 0
	- x > 0:
		~ y = x - 1
	- else:
		~ y = x + 1
}
---
(ink
	(content_block
		(paragraph
			(cond_block
				(cond_arm
					(binary (identifier) (number))
					(code (assignment name: (identifier) value: (number))))
				(cond_arm
					(binary (identifier) (number))
					(code (assignment name: (identifier) value: (binary (identifier) (number)))))
				(cond_arm
					(else)
					(code (assignment name: (identifier) value: (binary (identifier) (number)))))
			)
		)
	)
)


===
.CCC. Switch Blocks
===
{ x:
- 0: 	zero
- 1: 	one
- 2: 	two
- else: lots
}
---
(ink
	(content_block
		(paragraph
			(cond_block
				(cond_arm (identifier))
				(cond_arm (number) (paragraph (text)))
				(cond_arm (number) (paragraph (text)))
				(cond_arm (number) (paragraph (text)))
				(cond_arm (else) (paragraph (text)))
			)
		)
	)
)
